<h1>Products</h1>

<%output = "";
  output += "<table>"
  Product.all.each do |c|
    output += "<tr><td>#{c.id}</td><td> #{c.name}</td><td> #{c.price}</td><td> #{c.description}</td></tr>"
    end%>
    <%=output += "</table>"%>
